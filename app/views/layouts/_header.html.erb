<header>
  <nav>
    <ul>
      <% if logged_in? %>
        <li><%= link_to "sign out", logout_path, method: :delete , data: { confirm: 'ログアウトしますか？' } %></li>
      <% else %>
        <li><%= link_to "sign in", login_path %></li>
      <% end %>
    </ul>
  </nav>
</header>